---
import type { DropdownOptions } from "@utils/types";
interface Props {
    id: string;
    classes?: string;
    options: DropdownOptions[];
}

const { id, options, classes } = Astro.props;
---
<select id=`dropdownBox-${id}` class=`${classes} cursor-pointer flex h-10 w-[180px] items-center justify-between text-(--foreground) bg-(--background) rounded-xl border border-(--border) px-3 py-2 focus:outline-none focus:ring-2 focus:ring-(--ring) transition-all`>
    {options.map((el) => 
        <option class="w-full bg-(--accent) rounded-sm p-1" value={el.value}>{el.name}</option>
    )}
</select>
<script>
</script>
